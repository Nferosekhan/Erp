TRUNCATE `pairaccess`;
TRUNCATE `pairadjustments`;
TRUNCATE `pairbatch`;
TRUNCATE `pairbills`;
TRUNCATE `paircategory`;
TRUNCATE `paircontrols`;
TRUNCATE `paircustomers`;
TRUNCATE `pairdeliverychallans`;
TRUNCATE `pairduedates`;
TRUNCATE `pairenquirys`;
TRUNCATE `pairestimates`;
TRUNCATE `pairexpenses`;
TRUNCATE `pairfranchises`;
TRUNCATE `pairgst`;
TRUNCATE `pairhistory`;
TRUNCATE `pairinvoices`;
TRUNCATE `pairjobs`;
TRUNCATE `pairledgers`;
TRUNCATE `pairmainaccess`;
TRUNCATE `pairnothistory`;
TRUNCATE `pairnotifications`;
TRUNCATE `pairproducts`;
TRUNCATE `pairproformas`;
TRUNCATE `pairprojects`;
TRUNCATE `pairpropurchase`;
TRUNCATE `pairprosale`;
TRUNCATE `pairpurchaseorders`;
TRUNCATE `pairpurchasepayhistory`;
TRUNCATE `pairpurchasepayments`;
TRUNCATE `pairpurchasereceives`;
TRUNCATE `pairpurchasereturnpayhistory`;
TRUNCATE `pairpurchasereturnpayments`;
TRUNCATE `pairpurchasereturns`;
TRUNCATE `pairquotations`;
TRUNCATE `pairsalesorders`;
TRUNCATE `pairsalespayhistory`;
TRUNCATE `pairsalespayments`;
TRUNCATE `pairsalesreturnpayhistory`;
TRUNCATE `pairsalesreturnpayments`;
TRUNCATE `pairsalesreturns`;
TRUNCATE `pairsubcategory`;
TRUNCATE `pairtimesheet`;
TRUNCATE `pairusehistory`;